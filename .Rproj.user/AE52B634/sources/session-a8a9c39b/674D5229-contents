---
title: "Untitled"
lang: ja
format:
  pdf:
    latex-engine: lualatex
    include-in-header:
      text: |
        \usepackage{luatexja}
        \usepackage[haranoaji]{luatexja-preset} % ← これで和文フォント設定OK
        \usepackage{fontspec}
        \setmainfont{TeX Gyre Termes}          % 欧文（斜体あり）
        % 和文の擬似斜体が必要なら下記（見た目だけ傾ける）
        % \ltjsetparameter{jacharrange={-}} % なくても可
        % \renewcommand{\em}{\textsl}       % 斜体風強調の例（必要に応じて）
knitr:
  opts_chunk:
      'tbl-cap': false
---

## Quarto
This is writing method of *Quarto*.

#日本大学生物資源科学部

##アグリサイエンス学科
<https://quarto.org>.

|ID   |Name     |Math    |English |Favorite food|
|:---:|---------|-------:|-------:|-------------|
|1    |SONG     |15      |10      |Ramen        |
|2    |Yanai    |100     |100     |Cat food     |
|3    |Shigemura|80      |50      |Raw chicken  |
|4    |Wickham  |80      |90      |Lamb         |

![Fig1](figs/EC_difference_water_HP.pdf)

```{r}
# パッケージの読み込み
library(tidyverse)
# R内蔵データセットのirisを使った可視化
iris |>
  mutate(Species2 = recode(Species,
                           "setosa"     = "セトナ",
                           "versicolor" = "バーシクル",
                           "virginica"  = "バージニカ")) |>
  ggplot() +
  geom_point(aes(x = Sepal.Length, y = Sepal.Width, color = Species2)) +
  labs(x = "萼片の長さ (cm)", y = "萼片の幅 (cm)", color = "品種") 
  #theme_minimal(base_family = "HiraKakuProN-W3")
```



## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
library(gt)
data <- read.table("/cloud/project/example.csv",sep=",", comment.char="#", header=T) 

gt_table <- data |> 
  gt() |>
  fmt_number(columns = 2:5, decimals = 1)  |>
  tab_header(title="Table 1. Changes of air temperature.")|>
  tab_spanner(columns = 2:5, label = "Range")

gt_table
```

You can add options to executable code like this 

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).
